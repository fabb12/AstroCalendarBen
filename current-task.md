# Task Corrente

Modifica al manifest per l'apertura PWA:
- Cambiato `start_url` in `manifest.json` in `./index.html?vista=cielo`.
- L'app, quando installata come PWA, si avvierà ora direttamente sul planetario (planisfero) invece della visualizzazione standard "Stasera".
